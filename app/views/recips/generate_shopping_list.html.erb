<h1>Shopping List</h1>
<p>Amount of food items to buy: <span><%= @total_foods%></span></p>
<p>Total value of food needed: <span>$<%= @total_value%></span></p>

<table>
  <thead>
    <tr>
      <th>Food Name</th>
      <th>Quantity</th>
      <th>Price</th>
    </tr>
  </thead>
  <tbody>
    <% @shopping_list.each do |food_name, quantity| %>
      <tr>
        <td><%= food_name %></td>
        <td><%= quantity %><span> <%= Food.find_by(name: food_name).measurement_unit%></span></td>
        <td>$ <%= "#{quantity * Food.find_by(name: food_name).price}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

